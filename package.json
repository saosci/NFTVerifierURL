{
  "name": "@Dappy",
  "description": "NFT Verification Bot",
  "version": "0.0.1",
  "homepage": "https://op2.app",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/saosci/NFTVerifierURL"
  },
  "bugs": {
    "url": "https://github.com/saosci/NFTVerifierURL"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=16 <=18",
    "pnpm": "8"
  },
  "scripts": {
    "dev": "pnpm run --filter frontend dev",
    "build": "pnpm run --filter frontend build",
    "start": "pnpm run --filter frontend start",
    "lint": "pnpm run --filter frontend lint",
    "lint:fix": "pnpm run --filter frontend lint:fix",
    "prepare": "husky install"
  },
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "react": "^18.0.0",
        "react-dom": "^18.0.0",
        "next": "^13.0.0"
      }
    }
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2",
    "next": "^13.3.1",
    "typescript": "^5.0.4"
  }
}
